# page title
title: Webifier

# page header (is an object with title, description, background)
header:
  title: Webifier
  background: https://picsum.photos/1000
  description: |
    Turn any repository into a website!

# meta tags
meta:
  patch: index/meta.yml

# navbar management
nav:
  patch: index/nav.yml

# website configuration
config:
  patch: index/config.yml

